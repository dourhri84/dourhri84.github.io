# Optional: external form endpoint that the Survey/Feedback module POSTs to
# (e.g. a Formspree endpoint: https://formspree.io/f/xxxxxxx).
# If unset, survey responses are only saved in the visitor's browser (localStorage).
VITE_SURVEY_ENDPOINT=
